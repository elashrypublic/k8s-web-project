apiVersion: v1
kind: ConfigMap
metadata:
  name: fluentd-config
  namespace: banking
data:
  fluent.conf: |
    <source>
      @type tail
      path /var/log/containers/*.log
      pos_file /var/log/fluentd.pos
      tag kube.*
      format json
    </source>

    <match **>
      @type stdout
    </match>