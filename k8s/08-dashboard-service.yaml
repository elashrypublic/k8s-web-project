apiVersion: v1
kind: Service
metadata:
  name: banking-dashboard-service
  namespace: banking
spec:
  selector:
    app: banking-dashboard
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP